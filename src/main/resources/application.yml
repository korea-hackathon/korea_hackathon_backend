spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://3.36.99.156:3306/hackathon}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Qpwoeiru1768@}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        order_inserts: true
        order_updates: true

  batch:
    jdbc:
      initialize-schema: always