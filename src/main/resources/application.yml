spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/hackathon}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:qpwoeiru1768}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          batch_size: 300
        order_inserts: true
        order_updates: true

  batch:
    jdbc:
      initialize-schema: always
